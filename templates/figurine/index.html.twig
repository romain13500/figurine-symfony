{% extends 'base.html.twig' %}

{% block title %}User index{% endblock %}

{% block body %}
    <h1>User index</h1>
<div class="container"><table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for figurine in figurines %}
            <tr>
                <td>{{ figurine.id }}</td>
                <td>{{ figurine.name}}</td>
                <td>
                    <a href="{{ path('fig_show', {'id': figurine.id}) }}">show</a>
                    <a href="{{ path('figurines_edit', {'id': figurine.id}) }}">edit</a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    {# <a href="{{ path('app_users_new') }}">Create new</a> #}
    </div>
    
{% endblock %}